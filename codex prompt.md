# 项目骨架
```
你现在是一个资深 Android 开发工程师，请帮我从零创建一个 Android App 项目骨架，使用 Kotlin + Jetpack Compose + Material3。

项目名称：PocketLedger（中文名：“口袋账本”）

【技术要求】
- Kotlin
- Jetpack Compose UI
- Material3 设计体系
- 使用 MVVM 架构（ViewModel）
- 使用 Room 作为本地数据库（后续用）
- Navigation Compose 做页面导航

【整体目标】
先搭建完整项目结构 + 页面骨架，不需要实现具体业务逻辑。

【需要完成的内容】

1. 项目结构设计：
- data（数据层）
- ui（界面层）
- viewmodel（状态管理）
- navigation（导航）
- theme（主题）

2. 底部导航栏（Bottom Navigation）包含5个页面：
- HomeScreen（首页）
- RecordScreen（记录页）
- CalendarScreen（日历页）
- SavingsScreen（存钱小荷包）
- SettingsScreen（设置页）

3. 每个页面先用占位UI：
- 显示页面标题（居中）
- 简单卡片结构（Material3 Card）

4. 首页（HomeScreen）特殊要求：
- 顶部一个大卡片（深色渐变风格）
- 显示：
  - 当前结余（大字体）
  - 今日已花
  - 本月总支出
（先写死假数据）

5. 主题设计（非常重要）：
- 支持浅色 + 深色模式
- 主色：紫色（偏蓝紫）
- 使用 Material3 动态配色结构
- 卡片圆角：16dp以上
- 整体风格偏现代、简洁

6. Navigation：
- 使用 Navigation Compose
- 底部导航切换页面

7. 提供：
- 完整代码结构
- 关键文件代码
- 每个模块说明

【注意】
先不要实现数据库和业务逻辑，只搭UI骨架和结构。
代码要规范、可运行。
```

# 记一笔功能
```
请继续完善我当前的 Android App 项目。技术栈保持不变：Kotlin + Jetpack Compose + Material3 + Navigation Compose。

这次要实现“记一笔页面（核心功能）”，并调整首页交互逻辑。

【产品需求】
1. 首页不仅展示概要信息，还要支持“快速记一笔”。
2. 首页底部导航栏上方有一个悬浮加号按钮（Floating Action Button）。
3. 点击这个加号按钮后，跳转到“记一笔页面”。
4. “记录页”只负责查看历史记录，不承担新增记录入口。

【页面与交互要求】

一、HomeScreen（首页）
- 保留当前首页顶部大卡片，用于展示：
  - 当前结余
  - 今日已花
  - 本月总支出
- 页面底部仍然保留 Bottom Navigation
- 在页面底部中央、导航栏上方放置一个悬浮加号按钮（FAB）
- FAB 风格要精致，和整体紫蓝色主题一致
- 点击 FAB 后导航到 AddRecordScreen

二、AddRecordScreen（记一笔页面）
这是本次开发重点，请认真实现 UI 骨架和基础交互。

页面内容包括：
1. 顶部栏
- 返回按钮
- 页面标题：“记一笔”

2. 输入区域
- 消费内容输入框
  - label: 内容
  - placeholder: 例如“早饭”“地铁”“打印资料”

- 金额输入区域
  - 不是普通输入框，而是“计算器样式数字键盘”
  - 页面上方显示当前输入金额
  - 支持数字 0-9、小数点、删除
  - 先不用做复杂四则运算
  - 默认金额显示为 0

3. 类型选择
- 使用单选样式或 segmented button
- 三种类型：
  - 支出
  - 退款
  - 收入
- 默认选中“支出”

4. 日期时间
- 默认自动带出当前日期和当前时间
- 先只做展示和基础点击样式
- 后续再接真正的日期选择器和时间选择器
- 当前阶段允许写成可点击卡片：“日期：2026-04-01”“时间：08:35”

5. 自动分类展示
- 根据内容关键词自动分类
- 当前先做一个简单函数：
  - 包含“饭”“早餐”“午饭”“晚饭”“奶茶”“咖啡”"瑞幸" -> 餐饮
  - 包含“地铁”“公交”“打车”“电动车” -> 交通
  - 包含“教材”“打印”“文具”“GPT” -> 学习
  - 包含“电影”“游戏”“攀岩”“旅游”“火车”“高铁”“飞机”“门票” -> 娱乐
  - 包含“药”“医院” -> 医疗
  - 包含“衣”“裤”“鞋” -> 衣服
  - 包含“纸”“牙膏”“洗发水”“卫生巾” -> 日用品
  - 否则 -> 其他
- 在页面中展示“自动分类结果：xxx”
- 注意：分类只用于展示，不参与金额计算

6. 保存按钮
- 页面底部有一个主按钮：“保存”
- 当前点击后先不接数据库
- 暂时打印日志或弹 Toast：显示用户输入的内容、金额、类型、分类、日期时间
- 代码结构要为后续接 Room 数据库预留好

三、RecordScreen（记录页）
- 明确改为“仅查看记录”
- 删除或移除原本新增记录入口
- 暂时使用假数据列表展示几条记录
- 每条记录显示：
  - 内容
  - 金额
  - 类型
  - 分类
  - 时间

【代码结构要求】
1. 新增 AddRecordScreen.kt
2. 新增 AddRecordUiState 数据类
3. 新增 AddRecordViewModel
4. 导航中注册 AddRecordScreen 路由
5. 首页 FAB 触发导航
6. 分类逻辑单独提取成一个 utils 或 helper 函数，便于后续替换

【UI风格要求】
- 延续当前项目的现代化卡片风格
- 浅色背景 + 局部深色强调
- 圆角偏大
- 间距舒展
- 输入页不要做得像后台表单，要更像精致消费记录 App

【请同时输出】
1. 关键代码文件
2. 每个文件的作用说明
3. HomeScreen 如何接入 FAB
4. AddRecordScreen 的 Compose 结构说明
5. 我作为初学者需要重点看懂哪几部分
```
# 接入Room
# 梳理需求
1. 产品目标。
- [ ] 用户每消费一笔，都能快速录入。
- [ ] 系统自动记录时间、内容、金额、类型，并准确计算结余与统计结果。
- [ ] 系统以“日常流水小荷包”和“存钱小荷包”两个账户分离消费与储蓄。
- [ ] 整个 App 必须保证金额计算是确定性的，智能功能只能参与分类或展示，不能参与财务计算。

2. 账户体系需求。
整个 App 采用双账户模型。

- [ ] 第一个账户是“日常流水小荷包”。它是每月独立结算的主消费账户，用来记录支出、退款和收入。
	- [ ] 这个账户每月 1 号自动发放 2000 元生活费，作为该月预算基线。
	- [ ] 用户在日常生活中输入的绝大多数记录，都进入这个账户。
	- [ ] 这个账户允许结余为负数，也就是允许超支，但超支只作为本月历史存在，不结转到下个月。
- [ ] 第二个账户是“存钱小荷包”。它是独立储蓄账户，用于存放每月结余和用户主动储蓄的钱。
	- [ ] 它只能进不能出，不允许转回日常流水账户。

3. 日常流水小荷包的记录类型规则。

- [ ] 日常流水小荷包必须支持三种记录类型：支出、退款、收入。
- [ ] 支出表示正常花钱，比如吃饭、交通、购物，会增加本月支出，并减少当前结余。
- [ ] 退款不属于收入，它的本质是对已有支出的冲减。比如本月支出 1200，退款 200，则本月实际支出应为 1000。退款会增加当前结余，但不会记入“收入”。
- [ ] 收入表示额外入账，比如兼职费、临时转账等。收入会增加当前结余，但不影响“本月总支出”的统计口径。

4. 下面是月度结算规则。

- [ ] 每月 1 号，日常流水小荷包自动重置，并自动发放 2000 元。新月份的预算从 2000 重新开始，不受上个月超支影响。
- [ ] 每月月底，系统对该月日常流水小荷包进行结算。如果月底结余为正数，则自动转入“存钱小荷包”。如果月底结余为负数，则不报错，也不从存钱小荷包扣减，只作为该月历史记录保留。
这意味着每个月都是独立预算周期。超支不背到下个月；结余才会沉淀为储蓄。

5. 下面是记一笔功能需求。

- [ ] 记一笔是整个 App 的最高频核心功能。它应该从首页直接触发，不需要用户先切到记录页。
- [ ] 首页底部导航栏上方有一个中央悬浮加号按钮，点击后进入独立的“记一笔”页面。
- [ ] 记一笔页面需要包含以下内容：顶部返回栏；内容输入框；金额展示区；计算器样式数字键盘；记录类型选择器；日期展示；时间展示；自动分类结果；保存按钮。
- [ ] 内容输入框用于填写“早饭”“地铁”“打印资料”这样的自然语言描述。
- [ ] 金额输入采用计算器样式键盘，而不是普通系统输入框。需要支持数字 0 到 9、小数点和删除。默认金额显示为 0。当前阶段先不支持复杂四则运算，但要保证金额输入逻辑清晰，比如初始为 0 时输入数字应替换，已有小数点时不能重复输入，删除到空时应回到 0。
- [ ] 类型选择需要支持支出、退款、收入三种，默认选中支出。
- [ ] 日期和时间默认自动带出当前时间，且精确到分钟。当前阶段可以先做展示型卡片，后续再接入真正的日期选择器和时间选择器。
- [ ] 自动分类功能必须有，但分类不能由你手动选，也不能让 AI 介入金额计算。你最终选择的是“自动关键词分类”。系统根据内容字段里的关键词，自动将记录归类为餐饮、交通、学习、娱乐、医疗、衣服、日用品，无法匹配时归为其他。这个分类结果只参与展示和统计，不参与金额运算。
- [ ] 保存按钮当前的最终目标是将这条记录保存到本地数据库，并驱动首页和记录页联动更新。

6. 首页需求。

- [ ] 首页最核心的信息有三项：当前结余、今日已花、本月总支出。其中“当前结余”要作为最显眼的大数字放在顶部大卡片中。
- [ ] 首页页面结构上，需要有一块视觉重点很强的顶部大卡片，用深色渐变或深浅搭配风格展示核心数据。首页中下部可以保留简洁摘要区或辅助信息区。底部保留五个主页面的导航栏，并在导航栏上方中央悬浮一个加号按钮，用于快速记一笔。
- [ ] 首页的三个核心指标定义如下。当前结余，是基于本月预算、支出、退款和收入综合算出的当前剩余金额。今日已花，是当天支出总金额。月总支出是本月支出减去本月退款后的结果，不把收入计入其中。

7. 记录页需求。

 - [ ] 记录页是专门查看历史流水的页面，不再承担新增记录入口。它的职责就是让你按时间查看已保存记录。
 - [ ] 记录页中每条记录需要展示内容、金额、类型、分类、时间。后续还要支持修改历史记录，以及删除历史记录。删除时必须二次确认，避免误删。记录应按时间倒序展示，新记录在最上面。

8. 日历页需求。

它不仅是日期选择器，更是消费可视化界面。页面中应有可直接选择日期的月历视图，风格类似![[Pasted image 20260401081602.png|212]]

- [ ] 日历页的可视化规则是：每天的“消费强度”只看当天总支出金额，不考虑退款和收入。也就是说，这个页面更像消费热力图，用于直观展示你哪天花得多。最适合的设计是按颜色深浅或热度形式展示每日支出强弱。
- [ ] 点击某一天后，页面应先显示该日总支出，再展示该日的全部记录列表。也就是先给出“结论”，再展示明细。

9. 存钱小荷包页面需求。

存钱小荷包要有独立页面，专门展示累计存了多少钱。

- [ ] 这个页面至少要支持两类入账来源。第一类是系统自动转入的每月结余。第二类是用户手动存入，比如“红包 200”“奖金 3000”。这类手动存入不进入日常流水，也不影响本月支出统计，而是直接进入存钱小荷包。
- [ ] 存钱小荷包只允许入账，不允许转出。后续页面中可以展示累计余额、入账明细、入账来源和时间。

下面是设置页需求。

设置页当前不是核心功能区，但至少要为后续扩展预留位置。可以用于放置主题切换、数据导出、关键词分类规则说明、预算设置等功能。当前阶段只需要基础页面骨架即可。

下面是 UI 风格需求。

你明确希望 UI 风格接近成熟商业产品，而不是简单的学生练手项目。整体风格应当是现代、精致、卡片式、留白充分，主色调偏紫蓝。你不想要纯深色或纯浅色，而是希望“深浅搭配”。

具体上，可以采用“浅色页面背景 + 深色重点卡片”的混合风格。首页顶部大卡片、记一笔页面金额区等重点区域可用深蓝紫渐变强调；记录页、日历页等信息密集页面则以浅色背景和白色卡片为主。卡片圆角要大，整体间距要舒展，视觉上更接近成熟消费类 App。

下面是数据存储需求。

当前阶段你选择的是本地优先，不做云同步。也就是说，所有记录首先保存在手机本地数据库中，保证离线可用、成本低、结构稳定。未来可以再考虑云同步，但不作为第一阶段必须功能。

本地数据库建议使用 Room。需要有日常流水记录表，以及存钱小荷包记录表。后续还需要有查询能力，用于支持按日统计、按月统计、获取首页摘要、生成日历热力数据等。

下面是智能功能边界。

这个项目允许有“智能”，但智能必须严格受限。金额计算、结余计算、月结规则、存钱转入，这些财务逻辑必须由固定规则驱动，不能交给 AI 或模糊判断。自动分类可以用关键词匹配，未来也可以升级为 AI 分类，但 AI 只负责分类，不得参与金钱运算。

下面是当前开发状态和下一阶段目标。

从你目前的描述看，现阶段已经完成了项目骨架、页面导航、首页概览、独立记一笔页面、自动分类 helper、记录页假数据展示，以及保存按钮的 UI 交互。也就是说，你已经有了一个“能演示主流程”的原型。

接下来真正的重点不是再加新页面，而是把数据闭环打通。优先级最高的下一阶段需求是：把记一笔页面保存到 Room，本地真实存储；把记录页改为读取真实数据；把首页三项概览改为基于真实记录实时计算；再之后才是月初自动发放 2000、月底结余自动转入存钱小荷包、日历热力图和记录编辑删除等功能。

如果你愿意，我下一条可以直接把这些内容整理成一份更正式的《PocketLedger 项目需求文档 v1》，按“功能需求 / 非功能需求 / 业务规则 / 页面清单 / 数据表设计建议”的格式写给你，方便你直接发给 Codex 或放进仓库 README。
